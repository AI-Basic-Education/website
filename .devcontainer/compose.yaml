services:
  website:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/website:cached
      - node_modules:/website/node_modules
    command: "sleep infinity"

volumes:
  node_modules:
